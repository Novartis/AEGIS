sat:
  num_heads: 8
  num_layers: 6
  dim_hidden: 64
  dropout: 0.2
  structure_extractor: "khopgnn"
  # global_pool: "fc"
  global_pool: "fc-4clssum-i"
  # global_pool: "mean"
  encoding:
    abs_pe: "rw"
    abs_pe_dim: 20
  layer_norm: true
  sparse: false
  gnn_type: "pna2"
  k_hop: 3
  # se: "gnn"
  attn_type: "full"
  batch_norm: true
  node_weight: "degree"
