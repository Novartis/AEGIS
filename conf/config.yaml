
defaults:
  - _self_
  - compute: compute_cluster
  - model: sat
  - training: training_debug
  - seed: seed
  - debug: debug
  - dataset: dataset
  - paths: paths
    
hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}